systemd:

Создай виртуальное окружение командой - python.exe -m venv venv;
Активируй окружение командой - .\venv\Scripts\Activate.ps1;
Установи библиотеки командой - pip install -r requirements.txt;
Измени env_dist на .env и заполни его своими данными;